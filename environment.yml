name: stac-catalog
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.9
  - pip
  - pip:
      # Core STAC packages
      - pystac>=1.12.0
      - pystac-client>=0.8.0
      - rio-stac>=0.11.0
      # Geospatial packages
      - rasterio>=1.4.0
      - rio-cogeo
      - shapely
      # Data processing
      - pandas
      # Utilities
      - tqdm
